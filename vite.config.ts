import { defineConfig } from "vite";

// Deployed to GitHub Pages at https://eslawski.github.io/banana-run/
// The base path must match the repo name so built asset URLs resolve correctly.
export default defineConfig({
  base: "/banana-run/",
});
